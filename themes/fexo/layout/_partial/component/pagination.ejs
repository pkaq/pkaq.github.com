<% if (page.total > 1){ %>
  <div class="pagination">
    <nav class="page-nav">
      <%- paginator({
        prev_text: '前',
        next_text: '后',
        mid_size: 1
      }) %>
    </nav>
  </div>
<% } %>